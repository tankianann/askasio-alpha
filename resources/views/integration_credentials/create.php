<div class="page-heading page-heading-compact"><div><p class="breadcrumbs"><a href="/admin/integration-credentials">Chatbot API keys</a> <span>/</span> Create</p><h1>Create chatbot API key</h1><p class="muted">The key is shown once and can call only selected chatbots.</p></div></div>
<?php if(is_string($error)&&$error!==''):?><div class="alert alert-error" role="alert"><?= $escape($error) ?></div><?php endif;?>
<form class="panel form-panel" method="post" action="/admin/integration-credentials"><input type="hidden" name="_csrf" value="<?= $escape($csrfToken) ?>"><div class="form-group"><label for="name">Name</label><input id="name" name="name" maxlength="190" required value="<?= $escape($old['name']??'') ?>"></div><div class="form-group"><label for="expires">Expires (optional)</label><input id="expires" type="datetime-local" name="expires_at" value="<?= $escape($old['expires_at']??'') ?>"></div><fieldset><legend>Allowed chatbots</legend><?php foreach($chatbots as $chatbot):?><label class="checkbox-group"><input type="checkbox" name="chatbot_ids[]" value="<?= $chatbot['id'] ?>" <?= in_array($chatbot['id'],$old['chatbot_ids']??[],true)?'checked':'' ?>> <?= $escape($chatbot['name']) ?></label><?php endforeach;?></fieldset><div class="form-actions"><button class="button button-primary">Create credential</button></div></form>
